package org.example.spring_realization_transactions;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SpringRealizationTransactionsApplication {

    public static void main(String[] args) {
        SpringApplication.run(SpringRealizationTransactionsApplication.class, args);
    }

}
